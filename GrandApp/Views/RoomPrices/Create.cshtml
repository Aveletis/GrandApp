@model GrandApp.ViewModels.RoomPrices.CreateRoomPricesViewModel

@{
    ViewData["Title"] = "Create";
}

<h1>Create</h1>

<h4>RoomPrice</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="CostOneSM" class="control-label"></label>
                <input asp-for="CostOneSM" class="form-control" />
                <span asp-validation-for="CostOneSM" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="IdRoomCategory" class="control-label"></label>
                <select asp-for="IdRoomCategory" class ="form-control" asp-items="ViewBag.IdRoomCategory"></select>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
